---
import { Image } from "astro:assets";
const phoneNumber = '+5491159606813';
const defaultMessage = encodeURIComponent('Hola! Me gustaría recibir más información sobre sus productos.');
const whatsappLink = `https://wa.me/${phoneNumber}?text=${defaultMessage}`;
---

<section id="contacto" class="bg-gray-50 py-16 scroll-mt-[5rem]">
    <div class="container mx-auto px-6 lg:px-20">
      <div class="text-center mb-12">
        <h2 class="text-5xl font-bold text-gray-800">Contáctanos</h2>
        <p class="text-gray-700 mt-4 text-lg">
          <span class="text-green-700 font-semibold">¿Tenés alguna consulta?</span> Estamos aquí para ayudarte con cualquier duda sobre nuestros productos orgánicos.
        </p>
      </div>
  
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
        <div class="bg-white p-8 shadow-xl rounded-lg">
          <h3 class="text-3xl font-bold text-gray-800 mb-4">Información de Contacto</h3>
          <p class="text-gray-600 mb-6 ">
            Comunicate con nosotros mediante los siguientes canales. Te responderemos a la brevedad.
          </p>
          <div class="space-y-4 text-lg">
            <p class="flex flex-wrap items-center gap-2 text-gray-700">
              <Image src="/svg/Mail.svg" alt="Mail Svg" width={300} height={300} class="w-8" />
              <strong class="mr-1">Email:</strong> ecorganico.distribuidora@gmail.com
            </p>
            <p class="flex items-center gap-2 text-gray-700">
              <Image src="/svg/Phone.svg" alt="Phone Svg" width={300} height={300} class="w-8"/>
              
              <strong class="mr-1">Teléfono:</strong> +54 9 11 5960-6813
            </p>
            <a
              href={whatsappLink}
              target="_blank"
              rel="noopener noreferrer"
              class="flex justify-center items-center gap-2 w-full text-lg bg-green-200 text-green-900 px-4 py-2 rounded hover:bg-green-300 duration-200 mt-auto"
            >
              <img src="/svg/Wpp.svg" alt="WhatsApp Svg" class="w-8" loading="lazy">
              WhatsApp
            </a>
          </div>
        </div>
  
        <!-- <div class="bg-white p-8 shadow-xl rounded-lg">
          <h3 class="text-3xl font-bold text-gray-800 mb-4">Envíanos un Mensaje</h3>
          <form>
            <div class="mb-4 text-lg">
              <label for="nombre" class="block text-gray-700">Nombre</label>
              <input
                type="text"
                id="nombre"
                placeholder="Tu nombre"
                class="w-full p-3 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-green-500"
              />
            </div>
            <div class="mb-4 text-lg">
              <label for="email" class="block text-gray-700">Email</label>
              <input
                type="email"
                id="email"
                placeholder="Tu correo electrónico"
                class="w-full p-3 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-green-500"
              />
            </div>
            <div class="mb-4 text-lg">
              <label for="mensaje" class="block text-gray-700">Mensaje</label>
              <textarea
                id="mensaje"
                rows="4"
                placeholder="Escribe tu mensaje aquí..."
                class="w-full p-3 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-green-500 min-h-[80px] max-h-[100px]"
              ></textarea>
            </div>
            <button
              type="submit"
              class="w-full text-lg bg-green-200 text-green-900 px-4 py-2 rounded hover:bg-green-300 duration-200"
            >
              Enviar
            </button>
          </form>
        </div> -->
      </div>
    </div>
  </section>