---
import { getCollection } from "astro:content";
import ProductCard from "./Products/ProductCard.astro";

const products = await getCollection("products");
---

<section id="productos" class="scroll-mt-[5rem] py-14">
   <div class="w-full mx-auto space-y-5 mb-12">
      <h2 class="text-6xl uppercase font-bold text-gray-800 text-center">
         DiatomiD
      </h2>
      <p class="text-lg prose text-center mx-auto">
         Diatomid es un producto natural elaborado a partir de tierra de diatomeas, compuesto por los restos fosilizados de algas unicelulares llamadas diatomeas. Este mineral se utiliza principalmente como insecticida ecológico y fertilizante natural.
         Al ser un producto natural y no tóxico, Diatomid es seguro para humanos, animales y plantas. Sin embargo, es importante seguir las instrucciones de uso proporcionadas por el fabricante para garantizar su eficacia y seguridad.
      </p>
   </div>
   <div class="flex flex-wrap w-4/5 mx-auto justify-around">
      {
         products.map((product) => (
            <ProductCard product={product} />
         ))
      }
   </div>
</section>
