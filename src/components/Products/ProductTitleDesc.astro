---

const {product} = Astro.props
const {title, description} = product.data
const { body } = product
---


<div>
    <h3
        class="text-4xl font-semibold uppercase text-center"
    >
        {title}
    </h3>
    <p class="prose text-gray-900">{description}</p>
</div>